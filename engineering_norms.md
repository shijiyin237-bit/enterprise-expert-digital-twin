# 《AI 架构工程宪法 V6.0》

> **极致极简 · 泛化解耦 · 文档驱动 · 零机器味**
>
> 本宪法适用于所有 AI 驱动的软件工程项目。任何执行层 AI 在修改代码前，必须通读并遵守以下所有条款。

---

## 1. 极致极简与架构增生禁令 (Architecture Minimization & Anti-Proliferation)

### 1.1 严禁增生文件
- 在开发新功能时，执行层 AI **严禁**在未经指挥官明确授权的情况下创建任何新文件或零散脚本。
- 所有新功能必须优先通过对**现有文件**进行原位置改码与内聚重构来实现。

### 1.2 物理重叠审查
- 在产生"添加新文件"的想法前，必须先列出当前目录下所有相关的现有源文件，向指挥官进行核对。
- 只有在现有文件确实无法通过内聚方式承载新功能时，才允许创建新文件。

### 1.3 禁止零散工具
- 所有数据转换、格式化、物理拆解脚本，一律禁止作为孤立脚本散落在项目目录下。
- 必须归口封装进系统核心的流水线或 Manager 类中。

---

## 2. 零回归与防腐层原则 (Zero-Regression & ACL)

### 2.1 无损演进
- 修复 Bug 或升级特性时，只允许对受影响的代码块进行**最小化修改**。
- 绝对禁止为了引入新写法而重写已经过生产环境验证的稳定逻辑。

### 2.2 物理多租户隔离
- 多租户物理隔离底座（如向量数据库的独立 Collection、独立的索引文件）不可破坏。
- 任何功能开发必须向下兼容现有的租户物理目录划分。

### 2.3 数据模型防腐
- 模块间（网关、ETL、检索、生成）的调用与参数传递，必须使用领域模型中定义的 Pydantic 类作为**强契约防腐层**。
- 隔离外界数据格式变化对核心链路的污染。

---

## 3. Pydantic 护城河与反机器味清洗 (Strong Contracts & Anti-Sycophancy)

### 3.1 强契约流转
- 严禁直接在核心模块间传递无约束的 `raw dict`。
- 所有输入输出必须通过 Pydantic 强类型校验。

### 3.2 反 LLM 谄媚与清洗
- 所有接收大模型输出的文本字段，必须使用 Pydantic 的 `@field_validator` 进行正则清洗。
- 物理斩断并丢弃以下废话前缀（包括但不限于）：
  - "从上述对话中可以看出"
  - "作为一个AI"
  - "为您找到以下内容"
  - "根据我的分析"
  - "经过分析"
  - 以及其他一切带有"机器味"的解释性废话。

---

## 4. 彻底的泛化解耦 (Generalization & Zero-Shot Routing)

### 4.1 零硬编码
- 系统核心代码及 Prompt 中，绝对禁止出现特定企业名称、特定客户名或特定业务规则的硬编码。
- 所有业务相关的名词必须通过配置文件或环境变量动态加载。

### 4.2 配置与定义驱动
- 所有专家的人设、红线、意图路由词、工具挂载点，必须完全由配置文件（如 `profile.json`）动态加载。
- 实现 Zero-Shot（零样本）动态分类器与通用路由，代码不依赖任何特定业务场景。

### 4.3 死活数据物理解耦
- 系统必须物理区分以下两类数据：
  - **死知识（VectorRAG）**：从静态向量库检索，通过检索引擎进行双轨召回。
  - **活数据（MCP / 实时 API）**：实时业务数据（如订单、库存、数据库），必须走工具挂载，由意图探针动态决定是否分流。
- 两类数据在架构层面物理分离，互不干扰。

---

## 5. 数学与 AI 双重过滤管道 (Math-LLM Hybrid Pipeline)

### 5.1 统计锁死幻觉
- 严禁在未经过滤的情况下将海量脏数据或整篇文档"一口吞入"大模型进行盲目统计。

### 5.2 物理预处理
- 在提取行业黑话、专业术语或核心 SOP 节点等客观规律时，必须先使用 Python 原生统计库（如 `collections.Counter`）或中文分词工具进行物理层面的词频和结构统计。
- 锁定真实高频特征后，再将 Top-N 特征送入大模型执行语义价值提炼。

---

## 6. 面向指挥官与 AI 的极致注释协议 (Living Context Comments)

### 6.1 业务界碑 Docstring
- 所有类与函数必须配有大白话的中文 Docstring。
- Docstring 必须明确说明：
  - **输入（Input Contract）**
  - **输出（Output Contract）**
  - **副作用（Side Effects）**
- 确保技术细节零门垒。

### 6.2 逻辑节点透视
- 在代码核心流转处（如大模型调用、数据清洗拦截、RAG 融合重排），其上方必须空一行，并写入中文注释：
  ```python
  # [核心节点]：此处执行...
  ```

### 6.3 100% 汉化交互
- 终端控制台的所有打印日志、Web 交互大盘、报错拦截提示，必须 100% 使用专业、易懂的中文大白话。
- 系统必须捕获所有原生的英文 traceback 崩溃日志，将其转化为中文化错误包装，如：
  ```
  ❌ [致命拦截] JSON 剥壳失败，已触发自纠错...
  ```

---

## 7. LLMOps 全息可观测性与文档驱动开发 (Telemetry & DDD)

### 7.1 全链路日志记录
- 每一次 RAG 检索的命中切片、推理耗时、Prompt 长度、用户输入、意图路由状态，必须以 JSONL 格式异步写入日志文件。
- 日志格式遵循 PRD.md 中定义的接口契约。

### 7.2 活文档同步更新
- 项目根目录下的 `README.md` 是唯一的"小白级一键上手指南"。
- 任何涉及端口、启动方式、依赖更新的物理变动，执行层 AI 必须同步、无门槛地修改 `README.md`。
- 保障业务非技术人员能零配置一键运行。

### 7.3 文档驱动开发 (DDD - Document Driven Development)
- 系统维护三个核心文档，必须**实时更新**，不可滞后于代码：
  - **`PRD.md`（产品需求文档）**：定义系统要做什么、接口长什么样。开发过程中若产生冲突，必须由最高指挥官确认后亲自修改，执行层 AI 无权擅自变更 PRD 中的需求规格。
  - **`ARCHITECTURE.md`（系统架构图）**：定义文件调用关系、接口契约、核心运转链路、物理防线。每次架构变动后必须同步更新。
  - **`PROGRESS.md`（进度与状态机）**：记录已完成基线与当前正在执行的任务。这是**防断层的终极武器**，每次重构前后必须更新。
- 三文档联动机制：
  ```
  PRD.md (做什么) ──→ 驱动 ──→ ARCHITECTURE.md (怎么做)
                                      │
                                      ▼
                               PROGRESS.md (做到哪了)
  ```
### 8.1 执行层 AI（Cline）自审报告铁律
- 执行层 AI 在每次执行完修改（Edit）、删除、命令运行（Execute）后，必须在 Task Completed 前向指挥官输出一份中文 **【执行自审报告】**。
- 自审报告必须包含：
  1. 物理修改的文件及精确行号范围。
  2. 引入的外部依赖及导入链。
  3. 修改后的系统存活文件树是否有增生。
  4. 下一步建议执行的操作。

### 8.2状态机物理校验
- 任何代码层面的修改，执行层 AI 必须同步在 `PROGRESS.md` 中更新进度，在 `ARCHITECTURE.md` 中更新架构关系。
- 确保“参谋的战略蓝图”、“执行的代码实现”、“文档的状态记录”三位一体，严密对齐。

---

## 版本历史

| 版本 | 日期 | 变更说明 |
|------|------|---------|
| v1.0 | 2026-04-27 | 初始版本，确立数据契约和工程规范 |
| v2.0 | 2026-04-28 | 工业级重构，颁布零回归、Pydantic 护城河、数学+AI 双重过滤、彻底泛化解耦四大原则 |
| v3.0 | 2026-04-29 | 新增 LLMOps 可观测性铁律，强制要求日志记录与监控大盘 |
| v4.0 | 2026-05-05 | 剥离 C 端情感模块，全面转型 B 端企业专家数字孪生系统，升级意图路由与专业术语过滤机制 |
| **v6.0** | **2026-05-25** | **极致极简重构：新增架构增生禁令、文档驱动开发(DDD)、死活数据物理解耦；删除所有 SillyTavern/C端历史遗迹；全面泛化条款使其适用于任何 AI 项目** |
